class Player {
  constructor(token) {
    this.id = Math.floor(Math.random() * Math.floor(999999999));
    this.token = token;
    this.wins = [];
  }

  saveWinsToStorage() {

  }

  retrieveWinsFromStorage() {

  }
}
